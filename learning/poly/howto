$ poly
Poly/ML 5.0 Release
> fun f () = print "Hello World\n";
val f = fn : unit -> unit
> PolyML.export("hello", f);
val it = () : unit
> ^D
$ cc -o hello hello.o -lpolymain -lpolyml
$ ./hello
Hello World

---
$ poly
Poly/ML 5.0 Release
> PolyML.use "ext.sml"
> PolyML.export("poly", main);
> ^D
$ cc -o poly poly.o -lpolymain -lpolyml
